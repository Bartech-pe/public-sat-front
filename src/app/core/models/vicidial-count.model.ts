export interface VicidialCount {
  llamadas_atendidas: number;  // cantidad de llamadas atendidas hoy
  llamadas_totales: number;    // total de llamadas registradas
  llamadas_perdidas: number;   // llamadas abandonadas o perdidas
  llamadas_en_cola: number;    // llamadas actualmente en cola
}